import React from "react";
import { Icon } from "@ui-kitten/components";

export const HeadphonesIcon = (props) => (
  <Icon name="headphones-outline" {...props} />
);
export const StopSessionIcon = (props) => (
  <Icon name="stop-circle-outline" {...props} />
);
